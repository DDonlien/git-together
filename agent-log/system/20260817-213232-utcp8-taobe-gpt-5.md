# 20260817-213232-utcp8-taobe-gpt-5

## 用户原始 Prompt

```text
左边栏加一个 GitButler 的选项，把所有它的相关功能移入那里面
```

附件：`截屏2026-08-17 20.36.19.png`，展示 Global settings 中 GitButler access token 仍位于 General 的现状。

## 用户补充 Prompt

```text
合并回 MAIN 进行改动
```

## 模型与启动上下文

- 模型：GPT-5；当前运行环境未暴露更具体的公开模型标识。
- 启动分支：`git-together/main`
- 启动版本：`08e5f01d4d`；合并 `desktop/overview-split` 后的实现基准为 `94947e7cee`。
- 主 worktree：`/Users/taobe/Projects/GitHub/Personal/git-together/main`
- 来源 worktree：`/Users/taobe/Projects/GitHub/Personal/git-together/desktop-overview-split`
- 开始：2026-08-17 20:36:19 UTC+8
- 结束：2026-08-17 21:32:32 UTC+8
- 是否提交：是；功能提交为 `e79b16fc94`，本日志和协作规范同步另作纯文档提交。
- 是否推送：否；用户没有要求 push。

## 任务范围与需求归属

- 本次归属 `F-0.1.7 Repository Workspace GUI`。
- Global Settings 左栏在 General 后新增 GitButler 页面。
- GitButler 账号与 access token、上游 committer credit、兼容 `but` CLI、管理员 organizations 以及上游 Docs/Discord 资源统一移入该页。
- General 只保留 GitTogether 自身的编辑器、终端、更新和本地项目管理设置；通用 Git、AI Provider 和 forge integrations 保持独立页面。
- 用户头像与需要 GitButler 授权的入口改为直接打开 GitButler 页。

## 合并与并发边界

- 合并前读取 `desktop/overview-split` 相对 MAIN 的提交范围和 merge-tree 结果；待合入内容只有已提交的 `c251f4e3d7 Add Overview and Work Trees sidebar entries`，没有文本冲突。
- 在 MAIN 执行 `git merge --no-edit desktop/overview-split`，得到合并提交 `94947e7cee`。
- 来源 worktree 中未提交的并发改动没有被暂存、提交、清理或覆盖，来源分支保持不变。
- MAIN 同时存在另一项 `F-0.1.10 Subscription and API AI providers` 开发；其 `REQUIREMENTS.md`、AI、Tauri 和 Cargo 改动均保留在工作区。本次只交互式暂存 `F-0.1.7` 的 Requirements hunk，没有把 `F-0.1.10` 纳入提交。

## 协作规范同步

- 检查 canonical app template 后，将根 `AGENTS.md` 的通用规则和 TypeScript 规则同步到 `v1.4.0-260817-103337`。
- 保留 GitTogether 的项目专用规则、目录、Requirement 模型、远程边界和用户已有内容。

## 实现

- 新增 `GitButlerSettings.svelte`，复用现有 `@gitbutler/ui`、`AccessTokenSignIn`、`CliSymlinkSetup` 和 `OrganisationSettings`。
- 从 `GeneralSettings.svelte` 移出 GitButler profile/token 和兼容 CLI；从 `GitSettings.svelte` 移出 `gitbutler.gitbutlerCommitter`。
- 从 Global Settings 固定 footer 移出 GitButler Docs/Discord，并将管理员 Organizations 从独立左栏项移入 GitButler 页的条件区域。
- 更新设置页 ID、页面清单和授权/头像跳转，补充导航顺序与 Organizations 不再单列的回归测试。
- `VERSION` 从 `0.2.6` patch 递增为 `0.2.7`，因为这是向后兼容的用户可见界面整理；README、Requirements 和 Design 同步更新。

## 验证

- `git diff --check`：通过。
- 变更实现、Requirements、Design 与 README 的 Prettier check：通过；canonical `AGENTS.md` 单独使用 `git diff --check` 验证。
- `pnpm@10.20.0 --filter @gitbutler/desktop check`：0 errors，0 warnings。
- `NODE_OPTIONS=--localstorage-file=<temporary-file> pnpm@10.20.0 --filter @gitbutler/desktop test`：47 个 test files、387 个 tests 全部通过。
- 使用当前 MAIN 源码成功构建独立 `GitTogether Main QA.app`；构建使用 `disable-auto-updates`，只生成本地 QA `.app`，没有签名、发布或推送。
- 通过 Computer Use 实际打开 Global Settings：确认 GitButler 紧跟 General；General 不再显示 GitButler 项；Git stuff 只保留 auto-fetch；GitButler 页实际显示 access token、committer credit、兼容 `but` CLI 和 GitButler resources。未登录账号时管理员 Organizations 按设计隐藏，其条件渲染逻辑由 Svelte 检查覆盖。
- GUI 验收截图：`/Users/taobe/.codex/visualizations/2026/08/17/01a00fb8-fd9f-7391-9858-5801b4e44be4/gitbutler-settings-main-qa.jpeg`。

## 结果

- MAIN 已包含侧边栏合并提交 `94947e7cee` 与本功能提交 `e79b16fc94`。
- 本次功能已实现、自动化验证通过并完成实际 GUI 检查。
- 未推送远端；并发 `F-0.1.10` 工作区改动仍由其原任务持有。
