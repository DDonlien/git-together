# 20260811162133-utcp8-gpt-5

## 用户原始 Prompt

```text
Requirement 整理错误：Requirement 列表不是 Task list，里面应该是所有的 Feature，而不是你要做的事情。
查看一下我们的对话，看一下我想让他做的事情。
```

## 启动运行时的分支和版本

- 分支：`git-together/main`
- 版本：`2063f2f10e5e7a5319373119c93ceef0cc25c4f5`
- 同步：`origin/git-together/main` 与当前 HEAD 一致；`upstream/master` 保留为来源基线。

## 任务执行时间

- 开始：2026-08-11 16:21:33 UTC+8
- 结束：2026-08-11 16:31:54 UTC+8
- 是否提交：否

## 已阅读上下文

- 当前仓库的 `AGENTS.md`、`README.md` 和 `REQUIREMENTS.md`
- 引用对话 `Git Together` 的完整可读分页
- 引用对话中的 GitTogether 产品定位、Worktree 讨论和 UI Feature 描述

## 对话与行动记录

- 用户指出上一次把 Requirement 误写成了 Task list；Requirement 应列出所有 Feature，而不是 Agent 要执行的事项。
- 用户进一步明确：`0.0.x`、`0.1.x`、`0.2.x` 是版本线，不是只有三个 Phase；随后把移除不需要的 GitButler 本地 workflow 放到 `0.1.x`，新增本地 Feature 放到 `0.2.x`，Git Presence Server 放到 `0.3.x`。
- 引用对话确认 GitTogether 是 repository-first workspace for humans and AI agents。
- 引用对话确认核心 Feature 包括多仓库 Home、真实 branch/worktree、四区域 Workspace、Git Graph、Files、Git Details、Terminal、Thread/Agent、Presence/GPS 和安全 snapshot/merge。
- 引用对话确认自动化单位是 Work Session，而不是单个文件，也不是用户任务拆分；用户自行决定如何工作，GitTogether 负责隔离、记录影响范围、Presence 检查和安全合并。
- 将 `REQUIREMENTS.md` 改为四条 version line，并在每条 version line 下拆分多个 Phase；保留 Feature 清单语义，不写 implementation task list。
- 在 `AGENTS.md` 中补充 GitTogether 专用 Requirement / Feature 规则，避免后续再次把 Feature 拆成执行任务。

## 完成工作

- `REQUIREMENTS.md` 不再使用 `# 任务清单`、任务 checkbox 或 implementation checklist。
- 每个 `R-xxx` 现在是一个 Feature 定义，而不是待执行 task。
- 补回 Work Session、自动 snapshot、Presence-aware merge、真实 Git branch/worktree 和四区域 workspace 等产品语义。
- README 的需求文档入口改为“Feature 范围、状态和完成边界”。

## 验证

- 待交付前执行 `git diff --check`。
- 检查 `REQUIREMENTS.md` 中 Feature ID 和版本线归属。
- 检查文档中不存在任务 checkbox 或 Markdown requirement table。
- 检查 `REQUIREMENTS.md` 包含 4 条 version line、11 个 Phase，且没有任务 checkbox 或 Markdown requirement table。

## 备注

- 本次只修正文档语义，不修改源码目录、Git remote 或应用行为。
- `claude.md` 的删除由用户确认是有意改动；不恢复，仓库以自己的 `AGENTS.md` 为协作入口。
